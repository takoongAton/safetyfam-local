# 프로젝트 분석 보고서 — safetyfam-local

**작성일:** 2026-07-08
**저장소:** `safetyfam-local` (git, main 브랜치, 커밋 46개, 2025-04-17 ~ 2026-05-07)

---

## 1. 프로젝트 개요

**자녀안전지킴이(SafetyFam)** 서비스의 화면 퍼블리싱(HTML/CSS/JS 정적 마크업) 산출물 저장소입니다. 실제 동작하는 애플리케이션(백엔드/빌드 시스템 없음)이 아니라, **디자인 검수 및 개발 참고용 정적 화면 프로토타입**을 담고 있습니다. SKT/KT 등 통신사 제휴(알뜰폰 부가서비스)로 제공되는 "자녀 위치 확인/안전 모니터링" 서비스로 보이며, 앱(부모용/자녀용 웹뷰), 서비스 홈페이지, 유료 가입/해지 페이지 4개 모듈로 구성되어 있습니다.

- 총 용량: 약 64MB (대부분 `app/document`의 PPTX 1개 파일이 24.7MB 차지)
- 파일 구성: HTML 79개, PNG 146개, CSS 14개, JPG 9개, JS 7개, PPTX 1개
- 커밋 작성자: `takoong` 45건, `takoongAton` 1건 — 사실상 1인 작업

## 2. 디렉터리 구조

```
├── app/                  # 모바일 앱 웹뷰 화면 (부모용/자녀용)
│   ├── html/             # 부모용 화면 (00_FRCM ~ 09_ETC, 8개 카테고리)
│   ├── html_child/       # 자녀용 화면 (00_FRCM, 01_FRIN, 04_FRHO)
│   ├── css/              # reset, safetyfam_app(.ko), slick 캐러셀 CSS
│   ├── js/               # slick.js(캐러셀 라이브러리), temp.js
│   ├── images/           # 아이콘/목업 이미지 (81개)
│   ├── document/         # 기획 PPTX (자녀 초대 프로세스 UX 개선안)
│   └── index.html        # 전체 화면 인덱스(화면ID/뎁스/파일명 표 형태)
├── homepage/             # 서비스 소개 홈페이지 (공지사항, 1:1문의, 약관 등)
├── paid_signup/          # 유료 가입 신청 페이지 (실 운영 페이지 스냅샷으로 추정)
└── close_account/        # 부가서비스 해지/환불 페이지
```

각 최상위 모듈(`app`, `homepage`)의 `index.html`은 화면 설계서(스크린 ID·뎁스·파일명·구현 상태를 정리한 표)이자 화면 목록으로 가는 진입점 역할을 겸합니다.

## 3. 기술 스택 특징

- **순수 정적 HTML/CSS/JS** — 프레임워크, 번들러, 패키지 매니저(package.json) 없음. 빌드 과정 자체가 존재하지 않음.
- jQuery 1.8.2(로컬 라이브러리 패스 참조) + Slick Carousel(`slick.js`)을 이미지 슬라이더에 사용.
- `app/css/safetyfam_app.css`와 `safetyfam_app_ko.css`가 내용까지 동일(561줄, diff 없음) — 다국어 대응을 위해 분리해둔 것으로 보이나 현재는 중복 파일.
- CSS 변수(`--main-color`)를 활용한 간단한 테마 포인트 컬러 관리.
- 화면 파일명은 `FR + 카테고리약어(3~4자) + 일련번호[P]` 규칙 사용 (예: `FRHO_001` = 홈, `FRJO` = 가입/로그인, `FRSE` = 설정, `FRCS` = 고객센터, `FRCM` = 공통, `FRCL` = 자녀 위치, `FRMM` = 대화 모니터링, `FRIN` = 인트로). 접미사 `P`는 팝업(Popup) 화면을 의미.
- 화면 상태 배리에이션(정상/오류/데이터없음/로딩 등)을 별도 파일로 분기하여 관리하는 패턴이 반복됨 (`_00`, `_01`, `_02`, `_sos_01/02`, `_test`, `_sample` 등).

## 4. 모듈별 상세

### 4.1 `app/` — 앱 화면 (핵심 모듈)
- **부모용(`html/`)**: 00_FRCM(공통/알림), 01_FRIN(인트로), 02_FRJO(가입/로그인), 04_FRHO(홈), 04_FRHO_develop(홈 화면 고도화 실험 버전), 06_FRMM(대화 모니터링), 07_FRSE(설정), 08_FRCS(고객센터), 09_ETC(초대링크 브릿지 등 기타).
- **자녀용(`html_child/`)**: 00_FRCM, 01_FRIN, 04_FRHO만 존재 — 부모용 대비 화면 수가 훨씬 적음(부모용 39개 vs 자녀용 13개). 설정/고객센터 등은 자녀 화면에 아직 없음.
- 홈 화면(`04_FRHO`)이 가장 활발히 반복 수정된 영역으로, `_de`(기본)/`_min`(최소)/`_max`(최대) 등 하단 팝업 높이 배리에이션과 SOS(긴급호출) 관련 화면이 다수.
- `09_ETC/invite_link_bridge.html`(현재 IDE에서 열려 있던 파일)은 초대 코드 유효/만료 상태 두 케이스를 나란히 비교하는 웹뷰 브릿지 페이지 시안.
- `index.html`의 화면 목록표에는 아직 실제 파일이 연결되지 않은 항목(파일명 칸이 빈 `FRLO`, `FRCL`, `FRSE_002~010P` 등 로그인/자녀 위치/설정 대다수)이 많아 **전체 기획 대비 퍼블리싱 진행률이 낮음**을 알 수 있음.

### 4.2 `homepage/` — 서비스 소개 홈페이지
- 공지사항(목록/상세/없음/검색결과없음), 1:1 문의, 서비스 이용약관, 개인정보처리방침, 알뜰폰 가입시 추가약관 페이지로 구성.
- `index_test.html`이 별도로 존재 — 실험/검수용 사본으로 추정.

### 4.3 `paid_signup/` — 유료 가입 신청
- `safetyfam_join.html`, `safetyfam_join_260313.html`(날짜 버전 사본), `safetyfam_join_complete.html` 3개 파일.
- Google reCAPTCHA, Google Analytics(`UA-100705426-2`) 스크립트, 그리고 `safeconnect.co.kr`(본인인증/보안 솔루션 "SafeConnect") 연동 코드가 포함되어 있어 **실제 운영 중인 라이브 페이지의 스냅샷/사본**으로 보임(단순 디자인 시안이 아님).
- `location.href="http://210.219.175.129/ufiles/scupdate/1042/1.0.2/safeconnect.exe"` 처럼 **평문 HTTP로 실행파일(.exe)을 직접 다운로드 링크로 거는 코드**가 3개 파일에 반복 포함되어 있음 — 오래된 ActiveX/보안 프로그램 설치 방식의 잔재로, 보안·신뢰성 관점에서 위험 요소로 볼 수 있음(별도 보안 검토 권장 항목, 섹션 6 참고).
- `paid_signup/images/temp/`처럼 "temp(임시)" 폴더가 이미지 자산에도 존재.

### 4.4 `close_account/` — 부가서비스 해지/환불
- 단일 페이지(`close_account.html`, 타이틀: "부가서비스 환불 페이지")로 가장 단순한 모듈.

## 5. 프로젝트 진행 이력에서 읽히는 맥락 (git log 기반)

- 초기(2025-04-17) 커밋: "자녀안전지킴이 최초 업로드" 등 최초 스캐폴딩.
- 중반: 자녀용 초대코드 확인, 환불 신청 페이지, 하단 슬라이드/팝업 고도화, 긴급 SOS 버튼 유/무 케이스 추가 등 **홈 화면과 초대 플로우가 반복적으로 다듬어짐**.
- 최근 커밋(`fc1783d`, `732ef73`)은 "KT_취약계층 이용자보호 가입 재안내 팝업", "SKT, KT 추후 서비스 제공 문구 추가" — **통신사(SKT/KT) 대상 고지/약관 대응 작업**이 진행 중임을 시사.
- 가장 최근 3개 커밋은 "홈 2차 고도화" 관련으로, `app/document`의 PPTX(초대 프로세스 UX 개선안 v0.2)와 맥락이 이어짐 — 현재 활성 작업 트랙은 **자녀 초대 프로세스 및 홈 화면 고도화**로 판단됨.

## 6. 발견된 이슈 / 정리 포인트

| 구분 | 내용 |
|---|---|
| 중복 파일 | `safetyfam_app.css`와 `safetyfam_app_ko.css`가 완전히 동일한 내용(561줄) — 목적이 불분명하면 통합 검토 필요 |
| 버전 사본 난립 | `_test`, `_sample`, `_de/min/max`, `_260313` 등 날짜/용도별 사본 파일이 정리 없이 계속 누적되는 패턴 (특히 `04_FRHO_develop`, `paid_signup`) |
| 미완성 화면 | `app/index.html` 설계표 기준 로그인(FRLO), 자녀 위치 관리(FRCL), 설정(FRSE 다수), 자녀 등록/알림 수신 설정 등 다수 항목이 아직 파일 미연결 |
| 자녀용 화면 부족 | 자녀용(`html_child`)에는 설정/고객센터/공통 알림 일부만 있고 부모용 대비 화면 수가 3분의 1 이하 |
| 레거시 보안 패턴 | `paid_signup`의 SafeConnect 연동이 HTTP 평문 URL로 `.exe` 설치파일을 직접 안내 — 최신 브라우저 정책/보안 관점에서 재검토 권장 |
| `.DS_Store` 커밋 | 최상위 및 각 모듈 폴더에 `.DS_Store`가 저장소에 포함되어 있음 (`.gitignore` 부재) |
| README 부재 | `README.md`는 제목 한 줄(`# safetyfam-local`)뿐이라 신규 참여자를 위한 안내가 없음 |
| 용량 | `app/document`의 PPTX 파일 하나가 24.7MB로 전체 저장소 용량의 약 39%를 차지 (Git 저장소에 바이너리 대용량 기획 문서를 직접 커밋) |

## 7. 종합 평가

이 저장소는 실제 서비스 코드베이스가 아니라 **퍼블리싱/디자인 검수 단계의 정적 화면 산출물 모음**입니다. 화면 ID 체계와 상태별 배리에이션 관리 방식은 체계적이나, 진행 중인 스냅샷 특성상 임시/테스트 파일이 다수 혼재되어 있고 기획 대비 구현 완료율이 낮습니다. 현재 활성 작업은 "자녀 초대 프로세스"와 "홈 화면 고도화"이며, 통신사(SKT/KT) 대상 고지 문구 대응이 병행되고 있습니다. 향후 정리가 필요하다면 (1) 중복/사본 CSS·HTML 정리, (2) `.gitignore` 도입, (3) 대용량 기획 문서의 별도 관리(Git LFS 또는 외부 문서 도구 이관), (4) 자녀용 화면 세트 보강이 우선순위로 보입니다.
